# PSYCHO startup settings. Keyboard shortcuts can still adjust these while playing.
mouse_response = 60.0
hide_cursor = true

music_volume = 0.65
visual_fx = true
visual_strength = 0.65

# Experimental headphone effect.
audio_fx = true
audio_fx_strength = 0.5
